/*
#include "../geometry.h"

namespace TinyRender
{
template <>
template <>
vec<3, i32>::vec(const vec<3, float> &v) : x(i32(v.x + .5f)), y(i32(v.y + .5f)), z(i32(v.z + .5f))
{
}
template <>
template <>
vec<3, float>::vec(const vec<3, i32> &v) : x(v.x), y(v.y), z(v.z)
{
}
template <>
template <>
vec<2, i32>::vec(const vec<2, float> &v) : x(i32(v.x + .5f)), y(i32(v.y + .5f))
{
}
template <>
template <>
vec<2, float>::vec(const vec<2, i32> &v) : x(v.x), y(v.y)
{
}
}
*/